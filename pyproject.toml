[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dcc"
version = "0.1.0"
description = "Developer Command Center - a cross-platform command-line developer tool"
requires-python = ">=3.14"
dependencies = []